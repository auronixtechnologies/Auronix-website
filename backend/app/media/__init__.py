"""
Image handling: the upload optimisation pipeline and the base64 helpers used
to decode what the admin panel sends.

See app/media/images.py for why images are stored in Postgres rather than on
disk, and how they are served.
"""
